// Import Vue 3's createApp function and App component
import { createApp } from 'vue';
import App from './App.vue';  // Assuming your main component is called App.vue

// Create and mount the Vue app
createApp(App).mount('#app');
